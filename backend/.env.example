# Server
PORT=5000
FRONTEND_ORIGIN=http://localhost:5173

# PostgreSQL
DB_USER=queuelet
DB_HOST=127.0.0.1
DB_NAME=queuelet
DB_PASSWORD=queuelet123
DB_PORT=5434

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# SMTP (used by the email worker to actually send mail)
# For local testing you can use https://ethereal.email or a Mailtrap sandbox inbox
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
EMAIL_USER=your_smtp_user
